# GitReins — gitleaks configuration
# Auto-generated by 'gitreins init'. Edit to add project-specific exclusions.
# See: https://github.com/gitleaks/gitleaks#configuration

[allowlist]
description = "Narrowed — dependency/build dirs + log false-positive class only (specs/, docs/, *.md no longer whitelisted)"

paths = [
  '''.git/''',
  '''.gitreins/''',
  '''.gitreins/history/''',
  '''target/''',
  '''.*\.log$''',
]
